
USERNAME: Enter your username "{{USERNAME}}"
PASSWORD: Enter your password "{{PASSWORD}}"

vars_prompt:
    - name: enter the user name
      prompt: "{{USERNAME}}"
      private: false
    - name: password
      prompt: "{{PASSWORD}}"
      private: true
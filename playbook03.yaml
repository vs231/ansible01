- name: checking the variables
  hosts: web
  become: yes
  vars_files:
    - variables.yaml
  vars_prompt:
    - variablesprompt.yaml
  tasks:
    - name: checking the variables
      ansible.builtin.debug:
        msg: "username is {{USERNAME}} , password is {{PASSWORD}}"